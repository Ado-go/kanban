# Kanban

Kanban is a simple task management application based on the Kanban methodology. It allows users to organize tasks into columns representing their current status (e.g. To Do, In Progress, Done), helping to visualize workflow and improve productivity.

## Local start

```bash
npm install
npm run dev
```
